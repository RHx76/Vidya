import { db } from "@/lib/db";
import { redirect } from "next/navigation";

const CourseIdPage = ()=>{
  return (
    <div>
      Watch the course
    </div>
  )
}
 
export default CourseIdPage;